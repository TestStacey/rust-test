name: Test

on:
  schedule:
  - cron: '*/2 * * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Configure Git
    - run: echo ${{ github.repository }}
